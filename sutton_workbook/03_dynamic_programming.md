# 练习单 03：Dynamic Programming

来源：Sutton & Barto，第 4 章习题改写  
对应编号：`4.1, 4.2, 4.3, 4.4, 4.6, 4.7`

目标：

- 熟练写出策略评估与策略改进
- 区分 `V` 版本与 `Q` 版本的动态规划
- 理解无折扣 episodic 情形下的技术细节

---

## Q1. `[Ex 4.1]` 计算两个动作价值

在 Example 4.1 的 equiprobable random policy 下：

1. 计算 `q^π(11, down)`
2. 计算 `q^π(7, down)`
3. 说明为什么这两个值可以直接由一步转移和后继状态值写出来

### 作答区

- 

---

## Q2. `[Ex 4.2]` 新增状态后的价值变化

在 Example 4.1 的 `4x4 gridworld` 中：

1. 先按题目设定新增状态 `15`，求随机策略下的 `v^π(15)`。
2. 再把状态 `13` 的 `down` 动作改成通往 `15`，重新求 `v^π(15)`。
3. 解释第二次修改为什么会影响 `15` 的价值。

### 作答区

- 

---

## Q3. `[Ex 4.3]` Action-value 版本的策略评估

把 iterative policy evaluation 从 `v^π` 版本改写成 `q^π` 版本。

1. 写出 `q^π` 满足的 Bellman equation。
2. 写出 `q_0, q_1, q_2, ...` 的迭代更新形式。
3. 与状态值版本相比，`Q` 版本多显式保留了哪一层信息？

### 作答区

- 

---

## Q4. `[Ex 4.4]` 为什么迭代策略评估可能失效

考虑无折扣的 episodic task。

1. 如果某个策略有可能永远不终止，会给 `v^π(s)` 带来什么问题？
2. 这时 iterative policy evaluation 为什么可能不收敛或不适用？
3. 这个问题说明了什么条件对 DP 方法很重要？

### 作答区

- 

---

## Q5. `[Ex 4.6]` 用 `q*` 写 policy iteration

不要使用状态值函数 `V`，只用动作价值函数 `Q` 来描述一个完整的 policy iteration 框架。

1. 写出“评估”阶段要做什么。
2. 写出“改进”阶段要做什么。
3. 说明为什么用 `Q` 后可以省掉某些一步 lookahead 计算。

### 作答区

- 

---

## Q6. `[Ex 4.7]` `ε-soft` 策略下的 policy iteration

如果策略被限制为 `ε-soft`：

1. 初始化策略要满足什么条件？
2. 策略评估阶段本质上要不要变？
3. 策略改进阶段应如何从“纯贪心”改成“受约束的贪心”？

### 作答区

- 

